import 'package:flutter/material.dart';

class AddBluelock extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Add Bluelock"),
    );
  }

}