import 'package:flutter/material.dart';

class IssueToken extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Issue Token"),
    );
  }

}