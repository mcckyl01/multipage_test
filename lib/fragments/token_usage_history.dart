import 'package:flutter/material.dart';

class TokenUsageHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Token Usage History"),
    );
  }

}